{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Armee creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('armee') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
